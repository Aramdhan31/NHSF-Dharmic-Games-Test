"use client"

import { useState } from "react"
import { Header } from "@/components/header"
import { Footer } from "@/components/footer"
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"
import { Button } from "@/components/ui/button"
import { Badge } from "@/components/ui/badge"
import { isAdmin, getAdminName, getAllAdmins } from "@/lib/admin-config"
import { Users, Shield, Mail, CheckCircle } from "lucide-react"

export default function AdminDashboardPage() {
  const [currentUser] = useState({
    email: "", // Empty - no user logged in
    name: "",
  })

  const userIsAdmin = isAdmin(currentUser.email)
  const allAdmins = getAllAdmins()

  if (!userIsAdmin) {
    return (
      <div className="min-h-screen bg-gradient-to-br from-orange-50 to-red-50">
        <Header />
        <main className="py-20 px-4">
          <div className="container mx-auto max-w-2xl text-center">
            <div className="w-20 h-20 bg-red-100 rounded-full flex items-center justify-center mx-auto mb-6">
              <Shield className="w-10 h-10 text-red-600" />
            </div>
            <h1 className="text-4xl font-bold text-gray-900 mb-4">Access Denied</h1>
            <p className="text-xl text-gray-600 mb-8">This area is restricted to NHSF (UK) admins only.</p>
            <p className="text-gray-500">
              If you're an NHSF (UK) team member, please log in with your @nhsf.org.uk or .nhsf@gmail.com email address.
            </p>
          </div>
        </main>
        <Footer />
      </div>
    )
  }

  return (
    <div className="min-h-screen bg-gradient-to-br from-orange-50 to-red-50">
      <Header />

      <main className="py-12 px-4">
        <div className="container mx-auto max-w-4xl">
          <div className="text-center mb-8">
            <h1 className="text-4xl font-bold text-gray-900 mb-2">NHSF (UK) Admin Dashboard</h1>
            <p className="text-gray-600 mb-4">Welcome back, {getAdminName(currentUser.email)}!</p>
            <Badge className="bg-green-600 hover:bg-green-700">
              <Shield className="w-4 h-4 mr-1" />
              Admin Access Verified
            </Badge>
          </div>

          <div className="grid grid-cols-1 md:grid-cols-2 gap-6 mb-8">
            <Card>
              <CardHeader>
                <CardTitle className="flex items-center space-x-2">
                  <Users className="w-5 h-5 text-blue-600" />
                  <span>Quick Actions</span>
                </CardTitle>
              </CardHeader>
              <CardContent className="space-y-3">
                <Button asChild className="w-full bg-orange-600 hover:bg-orange-700">
                  <a href="/admin">🎮 Start New Game</a>
                </Button>
                <Button asChild variant="outline" className="w-full bg-white">
                  <a href="/admin">👥 Manage Users</a>
                </Button>
                <Button asChild variant="outline" className="w-full bg-white">
                  <a href="/admin">🤖 Generate Questions</a>
                </Button>
              </CardContent>
            </Card>

            <Card>
              <CardHeader>
                <CardTitle className="flex items-center space-x-2">
                  <CheckCircle className="w-5 h-5 text-green-600" />
                  <span>Your Admin Status</span>
                </CardTitle>
              </CardHeader>
              <CardContent className="space-y-3">
                <div className="flex items-center justify-between">
                  <span className="text-gray-600">Name:</span>
                  <span className="font-semibold">{getAdminName(currentUser.email)}</span>
                </div>
                <div className="flex items-center justify-between">
                  <span className="text-gray-600">Email:</span>
                  <span className="font-semibold text-sm">{currentUser.email}</span>
                </div>
                <div className="flex items-center justify-between">
                  <span className="text-gray-600">Role:</span>
                  <Badge className="bg-green-600">NHSF (UK) Admin</Badge>
                </div>
              </CardContent>
            </Card>
          </div>

          <Card>
            <CardHeader>
              <CardTitle className="flex items-center space-x-2">
                <Mail className="w-5 h-5 text-purple-600" />
                <span>NHSF (UK) Admins</span>
              </CardTitle>
            </CardHeader>
            <CardContent>
              <div className="grid grid-cols-1 md:grid-cols-2 gap-3">
                {allAdmins.map((admin) => (
                  <div key={admin.name} className="p-3 bg-gray-50 rounded-lg">
                    <div className="flex items-center justify-between mb-2">
                      <p className="font-medium text-sm">{admin.name}</p>
                      {admin.allEmails.includes(currentUser.email) && (
                        <Badge className="bg-blue-600 text-xs">You</Badge>
                      )}
                    </div>
                    <div className="space-y-1">
                      {admin.allEmails.map((email) => (
                        <p key={email} className="text-xs text-gray-600">
                          {email}
                          {email === currentUser.email && <span className="text-blue-600 ml-1">(current)</span>}
                        </p>
                      ))}
                    </div>
                  </div>
                ))}
              </div>
              <div className="mt-4 p-4 bg-blue-50 rounded-lg">
                <p className="text-sm text-blue-800">
                  <strong>Note:</strong> Each admin can use either their @nhsf.org.uk email or their .nhsf@gmail.com
                  email to access the admin panel.
                </p>
              </div>
            </CardContent>
          </Card>
        </div>
      </main>

      <Footer />
    </div>
  )
}
